<?xml version="1.0" encoding="UTF-8"?>
<ContentPage
	xmlns="http://xamarin.com/schemas/2014/forms"
	xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
	xmlns:language="clr-namespace:Trace.Localization;assembly=Trace"
	Padding="0,40,0,0"
	x:Class="Trace.HomeMasterPage"
	Title="{language:Translate Menu}"
	Icon="menu.png">
	<ContentPage.Content>
		<StackLayout
			VerticalOptions="FillAndExpand">
			<StackLayout
				Orientation="Horizontal"
				Grid.ColumnSpan="2">
				<Grid>
					<Image
						Source="{Binding PictureURL}"
						WidthRequest="48"
						HeightRequest="48" />
					<Image
						Source="photo_circle_mask"
						WidthRequest="48"
						HeightRequest="48" />
				</Grid>
				<StackLayout
					Orientation="Vertical">
					<Label
						Text="{Binding Name}"
						TextColor="#4BB166" />
					<Label
						Text="{Binding Email}"
						FontSize="Small"
						TextColor="Gray" />
				</StackLayout>
			</StackLayout>
			<ListView
				BackgroundColor="#4BB166"
				x:Name="listView"
				SeparatorColor="#24C121"
				VerticalOptions="FillAndExpand"
				SeparatorVisibility="Default">
				<ListView.ItemTemplate>
					<DataTemplate>
						<ImageCell
							Text="{Binding Title}"
							TextColor="White"
							ImageSource="{Binding IconSource}" />
					</DataTemplate>
				</ListView.ItemTemplate>
			</ListView>
		</StackLayout>
	</ContentPage.Content>
</ContentPage>