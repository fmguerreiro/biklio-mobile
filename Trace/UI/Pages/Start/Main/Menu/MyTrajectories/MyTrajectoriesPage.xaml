<?xml version="1.0" encoding="UTF-8"?>
<ContentPage
	xmlns="http://xamarin.com/schemas/2014/forms"
	xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
	xmlns:locals="clr-namespace:Trace;assembly=Trace"
	xmlns:language="clr-namespace:Trace.Localization;assembly=Trace"
	x:Class="Trace.MyTrajectoriesPage"
	Title="{language:Translate MyRoutes}">
	<ContentPage.Resources>
		<ResourceDictionary>
			<locals:ImageToRouteCheckConverter
				x:Key="converter" />
		</ResourceDictionary>
	</ContentPage.Resources>
	<ContentPage.Content>
		<ListView
			ItemsSource="{Binding Trajectories}"
			Header="{Binding Summary}"
			SeparatorVisibility="None"
			ItemSelected="OnSelection">
			<ListView.HeaderTemplate>
				<DataTemplate>
					<StackLayout
						Spacing="0">
						<Label
							FontAttributes="Bold"
							XAlign="Center"
							Text="{Binding .}" />
					</StackLayout>
				</DataTemplate>
			</ListView.HeaderTemplate>
			<ListView.ItemTemplate>
				<DataTemplate>
					<ImageCell
						Text="{Binding Id}"
						Detail="{Binding TotalDistanceMeters, StringFormat='{0} m'}"
						ImageSource="{Binding WasTrackSent, Converter={StaticResource converter}}">
					</ImageCell>
				</DataTemplate>
			</ListView.ItemTemplate>
		</ListView>
	</ContentPage.Content>
</ContentPage>